# Paper case inputs

This directory contains the reproducibility inputs used for the manuscript
figures: `.dfn` decks, coordinate text files, small analysis scripts, and
derived tabular metadata. Generated SVG/PNG figures, meshes, and runtime logs
are intentionally excluded from the source distribution; the executable can
recreate the mesh outputs from each deck.
